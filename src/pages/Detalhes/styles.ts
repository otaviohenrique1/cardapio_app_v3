import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 10,
    alignItems: "center"
  },
  foto: {
    width: 300,
    height: 300,
    marginRight: 10,
    borderRadius: 5,
  }
});
export default styles;
